import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
	container: { height: "100%", backgroundColor: "#040404", padding: 18 },
	baseText: {
		color: "#FFF",
	},
	pageTitle: {
		color: "#FFF",
		fontSize: 24,
		fontWeight: "600",
	},
});
